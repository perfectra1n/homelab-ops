apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vm-cluster
  namespace: argocd
spec:
  project: default
  source:
    chart: victoria-metrics-cluster
    repoURL: https://victoriametrics.github.io/helm-charts
    targetRevision: '*'
    helm:
      releaseName: vm-cluster
      # For the securitycontext, make sure it matches the uid/gid of the NFS server
      values: |
        vminsert:
          persistentVolume:
            enabled: true
            existingClaim: vminsert-pvc
          service:
            type: LoadBalancer
        vmstorage:
          persistentVolume:
            enabled: true
            existingClaim: vmstorage-pvc
          service:
            type: LoadBalancer
          securityContext:
            runAsUser: 65534
            runAsGroup: 65534
        vmselect:
          service:
            type: LoadBalancer
  destination:
    server: "https://kubernetes.default.svc"
    namespace: victoriametrics
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true 